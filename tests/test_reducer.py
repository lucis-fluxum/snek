class TestReducer:
    pass
    # TODO: Write this once we're able to get a compiled list from the reducer
    # def test_reduce(self, mocker):
    #     mock_repository_json(mocker)
    #     resolver = Resolver()
    #     reducer = Reducer()
